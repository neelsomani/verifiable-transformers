{
  "seed": 1337,
  "train_batch_size_per_device": 2,
  "eval_batch_size_per_device": 2,
  "gradient_accumulation_steps": 16,
  "learning_rate": 0.00001,
  "weight_decay": 0.0,
  "max_grad_norm": 1.0,
  "adam_beta1": 0.9,
  "adam_beta2": 0.95,
  "adam_epsilon": 1e-8,
  "warmup_steps": 200,
  "max_steps": 5000,
  "lr_scheduler_type": "cosine",
  "eval_steps": 250,
  "save_steps": 500,
  "logging_steps": 25,
  "save_total_limit": 3,
  "bf16": true,
  "fp16": false,
  "gradient_checkpointing": false,
  "dataloader_num_workers": 4,
  "calibration_steps": 500,
  "transition_steps": 500,
  "gap_steps": 100,
  "ema_momentum": 0.9,
  "fold_validation_batch_size": 1,
  "fold_max_abs_tolerance_fp32": 0.02,
  "fold_relative_l2_tolerance_fp32": 1e-5,
  "fold_top1_agreement_min": 1.0,
  "fold_eval_loss_delta_tolerance": 0.001,
  "bandnorm_eval_loss_gate": 3.318,
  "decision_rule": "Choose norm-free only if its final post-fold OpenWebText eval loss is below the preregistered BandNorm-only comparator of 3.3180; otherwise retain BandNorm."
}
